# Configuração `docker-compose` para banco `MySQL`.

## Funcionamento

* Baixe o repositório e execute o comando abaixo:

```bash
  # Windows
  docker-compose up --build -D

  # Linux
  sudo docker-compose up --build --D
```